<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WslContainerDesktop.Views.ImagesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WslContainerDesktop.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WslContainerDesktop.Models"
    x:Name="Root"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BytesToSizeConverter x:Key="BytesToSize" />
        <converters:RelativeTimeConverter x:Key="RelativeTime" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:EmptyToVisibilityConverter x:Key="EmptyToVis" />
        <converters:BoolToSelectionModeConverter x:Key="BoolToSelMode" />
    </Page.Resources>

    <Grid Margin="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="Images" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>

        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8"
            Visibility="{x:Bind ViewModel.IsSelectionMode, Mode=OneWay, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
            <Button Command="{x:Bind ViewModel.PullCommand}" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE896;" />
                    <TextBlock Text="Pull image" />
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.BuildCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE9F9;" />
                    <TextBlock Text="Build" />
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.RefreshCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE72C;" />
                    <TextBlock Text="Refresh" />
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.PruneCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE74D;" />
                    <TextBlock Text="Prune" />
                </StackPanel>
            </Button>
            <ToggleButton IsChecked="{x:Bind ViewModel.IsSelectionMode, Mode=TwoWay}" ToolTipService.ToolTip="Select multiple images">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE762;" />
                    <TextBlock Text="Select" />
                </StackPanel>
            </ToggleButton>
        </StackPanel>

        <!--  Bulk-action bar (shown while selecting)  -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8"
            Visibility="{x:Bind ViewModel.IsSelectionMode, Mode=OneWay, Converter={StaticResource BoolToVis}}">
            <TextBlock
                VerticalAlignment="Center"
                FontWeight="SemiBold"
                Text="{x:Bind ViewModel.SelectionSummary, Mode=OneWay}" />
            <Button Click="BulkRemove_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE74D;" />
                    <TextBlock Text="Remove selected" />
                </StackPanel>
            </Button>
            <Button Click="BulkCancel_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE711;" />
                    <TextBlock Text="Cancel" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!--  Table: column header (wide only) + list  -->
        <Grid Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup>
                    <VisualState x:Name="HeaderWide">
                        <VisualState.StateTriggers>
                            <converters:ControlSizeTrigger MinWidth="720" TargetElement="{Binding ElementName=Root}" />
                        </VisualState.StateTriggers>
                    </VisualState>
                    <VisualState x:Name="HeaderNarrow">
                        <VisualState.StateTriggers>
                            <converters:ControlSizeTrigger MaxWidth="720" TargetElement="{Binding ElementName=Root}" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <Setter Target="HeaderBorder.Visibility" Value="Collapsed" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

        <Border
            x:Name="HeaderBorder"
            Grid.Row="0"
            Padding="16,8,20,8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="6,6,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.6*" MinWidth="150" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="90" />
                    <ColumnDefinition Width="110" />
                    <ColumnDefinition Width="92" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="REPOSITORY" />
                <TextBlock Grid.Column="1" Style="{ThemeResource CaptionTextBlockStyle}" Text="TAG" />
                <TextBlock Grid.Column="2" Style="{ThemeResource CaptionTextBlockStyle}" Text="IMAGE ID" />
                <TextBlock Grid.Column="3" Style="{ThemeResource CaptionTextBlockStyle}" Text="SIZE" />
                <TextBlock Grid.Column="4" Style="{ThemeResource CaptionTextBlockStyle}" Text="CREATED" />
                <TextBlock
                    Grid.Column="5"
                    HorizontalAlignment="Right"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="ACTIONS" />
            </Grid>
        </Border>

        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,6,6">
            <Grid>
                <ListView
                    x:Name="ImagesList"
                    Padding="0,4"
                    ItemsSource="{x:Bind ViewModel.Images, Mode=OneWay}"
                    SelectedItem="{x:Bind ViewModel.Selected, Mode=TwoWay}"
                    SelectionChanged="List_SelectionChanged"
                    SelectionMode="{x:Bind ViewModel.IsSelectionMode, Mode=OneWay, Converter={StaticResource BoolToSelMode}}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="16,6,20,6" />
                            <Setter Property="MinHeight" Value="48" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:ImageInfo">
                            <Grid x:Name="RowRoot">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup>
                                        <VisualState x:Name="RowWide">
                                            <VisualState.StateTriggers>
                                                <converters:ControlSizeTrigger MinWidth="680" TargetElement="{Binding ElementName=RowRoot}" />
                                            </VisualState.StateTriggers>
                                        </VisualState>
                                        <VisualState x:Name="RowNarrow">
                                            <VisualState.StateTriggers>
                                                <converters:ControlSizeTrigger MaxWidth="680" TargetElement="{Binding ElementName=RowRoot}" />
                                            </VisualState.StateTriggers>
                                            <VisualState.Setters>
                                                <Setter Target="WideGrid.Visibility" Value="Collapsed" />
                                                <Setter Target="NarrowPanel.Visibility" Value="Visible" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>

                                <Grid x:Name="WideGrid">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2.6*" MinWidth="150" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="120" />
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="110" />
                                    <ColumnDefinition Width="92" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Repository}"
                                    TextTrimming="CharacterEllipsis" />
                                <TextBlock
                                    Grid.Column="1"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind Tag}" />
                                <TextBlock
                                    Grid.Column="2"
                                    VerticalAlignment="Center"
                                    FontFamily="Consolas"
                                    FontSize="12"
                                    Text="{x:Bind ShortId}" />
                                <TextBlock
                                    Grid.Column="3"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind Size, Converter={StaticResource BytesToSize}}" />
                                <TextBlock
                                    Grid.Column="4"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind CreatedUtc, Converter={StaticResource RelativeTime}}" />

                                <StackPanel
                                    Grid.Column="5"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal"
                                    Spacing="4">
                                    <Button
                                        Padding="7,5"
                                        Click="RunButton_Click"
                                        ToolTipService.ToolTip="Run">
                                        <FontIcon FontSize="14" Glyph="&#xE768;" />
                                    </Button>
                                    <Button Padding="7,5" ToolTipService.ToolTip="More">
                                        <FontIcon FontSize="14" Glyph="&#xE712;" />
                                        <Button.Flyout>
                                            <MenuFlyout Opening="MoreFlyout_Opening">
                                                <MenuFlyoutSubItem Text="Run profile">
                                                    <MenuFlyoutSubItem.Icon>
                                                        <FontIcon Glyph="&#xE768;" />
                                                    </MenuFlyoutSubItem.Icon>
                                                </MenuFlyoutSubItem>
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem Click="TagMenu_Click" Text="Tag" />
                                                <MenuFlyoutItem Click="PushMenu_Click" Text="Push">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE898;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem Click="InspectMenu_Click" Text="Inspect" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem Click="RemoveMenu_Click" Text="Remove">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE74D;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>
                                </StackPanel>
                                </Grid>

                                <!--  Narrow: stacked card layout (shown when the row is too narrow for columns)  -->
                                <StackPanel x:Name="NarrowPanel" Spacing="8" Visibility="Collapsed">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <FontIcon FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Glyph="&#xEB9F;" />
                                        <TextBlock VerticalAlignment="Center" FontWeight="SemiBold" Text="{x:Bind Repository}" TextTrimming="CharacterEllipsis" />
                                    </StackPanel>

                                    <Grid ColumnSpacing="8" RowSpacing="3">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="76" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock Grid.Row="0" Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="TAG" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind Tag}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Row="1" Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="IMAGE ID" />
                                        <TextBlock Grid.Row="1" Grid.Column="1" FontFamily="Consolas" FontSize="12" Text="{x:Bind ShortId}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Row="2" Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="SIZE" />
                                        <TextBlock Grid.Row="2" Grid.Column="1" Text="{x:Bind Size, Converter={StaticResource BytesToSize}}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Grid.Row="3" Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="CREATED" />
                                        <TextBlock Grid.Row="3" Grid.Column="1" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="{x:Bind CreatedUtc, Converter={StaticResource RelativeTime}}" />
                                    </Grid>

                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <Button Padding="7,5" Click="RunButton_Click" ToolTipService.ToolTip="Run">
                                            <FontIcon FontSize="14" Glyph="&#xE768;" />
                                        </Button>
                                        <Button Padding="7,5" ToolTipService.ToolTip="More">
                                            <FontIcon FontSize="14" Glyph="&#xE712;" />
                                            <Button.Flyout>
                                                <MenuFlyout Opening="MoreFlyout_Opening">
                                                    <MenuFlyoutSubItem Text="Run profile">
                                                        <MenuFlyoutSubItem.Icon>
                                                            <FontIcon Glyph="&#xE768;" />
                                                        </MenuFlyoutSubItem.Icon>
                                                    </MenuFlyoutSubItem>
                                                    <MenuFlyoutSeparator />
                                                    <MenuFlyoutItem Click="TagMenu_Click" Text="Tag" />
                                                    <MenuFlyoutItem Click="PushMenu_Click" Text="Push">
                                                        <MenuFlyoutItem.Icon>
                                                            <FontIcon Glyph="&#xE898;" />
                                                        </MenuFlyoutItem.Icon>
                                                    </MenuFlyoutItem>
                                                    <MenuFlyoutItem Click="InspectMenu_Click" Text="Inspect" />
                                                    <MenuFlyoutSeparator />
                                                    <MenuFlyoutItem Click="RemoveMenu_Click" Text="Remove">
                                                        <MenuFlyoutItem.Icon>
                                                            <FontIcon Glyph="&#xE74D;" />
                                                        </MenuFlyoutItem.Icon>
                                                    </MenuFlyoutItem>
                                                </MenuFlyout>
                                            </Button.Flyout>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <StackPanel
                    Margin="0,60,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Spacing="8"
                    Visibility="{x:Bind ViewModel.Images.Count, Mode=OneWay, Converter={StaticResource EmptyToVis}}">
                    <FontIcon
                        HorizontalAlignment="Center"
                        FontSize="40"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xEB9F;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="No images. Pull one to get started." />
                </StackPanel>

                <ProgressBar
                    VerticalAlignment="Top"
                    IsIndeterminate="True"
                    Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </Border>
        </Grid>
    </Grid>
</Page>
