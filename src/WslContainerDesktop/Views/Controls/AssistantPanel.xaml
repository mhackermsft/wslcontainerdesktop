<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WslContainerDesktop.Views.Controls.AssistantPanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WslContainerDesktop.Helpers"
    xmlns:local="using:WslContainerDesktop.Views.Controls"
    xmlns:viewmodels="using:WslContainerDesktop.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid
        MinWidth="380"
        Background="{ThemeResource SolidBackgroundFillColorBaseBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Padding="16" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel Spacing="4">
                <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Container AI Assistant" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Scoped to WSL containers, compose, and installed k3s tools."
                    TextWrapping="Wrap" />
                <Border
                    Padding="8,4"
                    HorizontalAlignment="Left"
                    Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                    CornerRadius="{StaticResource ControlCornerRadius}">
                    <StackPanel
                        VerticalAlignment="Center"
                        Orientation="Horizontal"
                        Spacing="8">
                        <Grid
                            Width="8"
                            Height="8"
                            VerticalAlignment="Center">
                            <Ellipse
                                Fill="{ThemeResource SystemFillColorSuccessBrush}"
                                Visibility="{x:Bind BoolToVisibility(ViewModel.IsProviderAvailable), Mode=OneWay}" />
                            <Ellipse
                                Fill="{ThemeResource SystemFillColorCautionBrush}"
                                Visibility="{x:Bind local:AssistantPanel.InvertBoolToVisibility(ViewModel.IsProviderAvailable), Mode=OneWay}" />
                        </Grid>
                        <TextBlock
                            AutomationProperties.Name="Active AI provider and model"
                            Style="{StaticResource CaptionTextBlockStyle}"
                            Text="{x:Bind ViewModel.ProviderLabel, Mode=OneWay}" />
                    </StackPanel>
                </Border>
            </StackPanel>
            <Button
                AutomationProperties.AutomationId="AssistantNewChatButton"
                Grid.Column="1"
                VerticalAlignment="Top"
                AutomationProperties.Name="Start a new chat"
                Command="{x:Bind ViewModel.NewChatCommand}"
                Style="{StaticResource SubtleButtonStyle}"
                ToolTipService.ToolTip="Start a new chat">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="16" Glyph="&#xE710;" />
                    <TextBlock Text="New chat" />
                </StackPanel>
            </Button>
            <Button
                AutomationProperties.AutomationId="AssistantCloseButton"
                Grid.Column="2"
                Width="40"
                Height="32"
                Padding="0"
                VerticalAlignment="Top"
                AutomationProperties.Name="Close assistant"
                Click="Close_Click"
                Style="{StaticResource SubtleButtonStyle}">
                <FontIcon Glyph="&#xE711;" />
            </Button>
        </Grid>

        <StackPanel Grid.Row="1" Padding="16,0,16,8" Spacing="8">
            <InfoBar
                x:Name="AssistantFeedbackBar"
                AutomationProperties.AutomationId="AssistantFeedbackInfoBar"
                CloseButtonClick="AssistantFeedbackBar_CloseButtonClick"
                IsClosable="True"
                IsOpen="{x:Bind ViewModel.Feedback.IsVisible, Mode=OneWay}"
                Message="{x:Bind ViewModel.Feedback.Message, Mode=OneWay}"
                Severity="{x:Bind converters:AiFeedbackDisplay.ToInfoBarSeverity(ViewModel.Feedback.Severity), Mode=OneWay}"
                Title="{x:Bind ViewModel.Feedback.Title, Mode=OneWay}" />
            <Expander
                AutomationProperties.AutomationId="AssistantFeedbackDetailsExpander"
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Header="Technical details"
                Visibility="{x:Bind BoolToVisibility(ViewModel.Feedback.HasTechnicalDetails), Mode=OneWay}">
                <StackPanel Spacing="8">
                    <TextBlock
                        AutomationProperties.AutomationId="AssistantFeedbackDetailsText"
                        FontFamily="Cascadia Mono, Consolas"
                        Style="{StaticResource CaptionTextBlockStyle}"
                        IsTextSelectionEnabled="True"
                        Text="{x:Bind ViewModel.Feedback.TechnicalDetails, Mode=OneWay}"
                        TextWrapping="Wrap" />
                    <Button
                        AutomationProperties.AutomationId="CopyAssistantFeedbackDetailsButton"
                        Command="{x:Bind ViewModel.CopyFeedbackDetailsCommand}"
                        Content="Copy details" />
                </StackPanel>
            </Expander>
            <TextBlock
                AutomationProperties.AutomationId="AssistantTimelineLimitNotice"
                Style="{StaticResource CaptionTextBlockStyle}"
                Text="Older activity was omitted. Showing the latest 100 entries; individual entries may be truncated."
                TextWrapping="Wrap"
                Visibility="{x:Bind BoolToVisibility(ViewModel.HasOmittedActivity), Mode=OneWay}" />
        </StackPanel>

        <ListView
            x:Name="TranscriptList"
            AutomationProperties.AutomationId="AssistantTranscriptList"
            AutomationProperties.Name="Conversation and tool activity"
            Grid.Row="2"
            Padding="16,0,16,12"
            ItemsSource="{x:Bind ViewModel.Messages, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="viewmodels:AssistantTimelineEntry">
                    <Border
                        Margin="0,0,0,8"
                        Padding="12"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="{StaticResource ControlCornerRadius}">
                        <StackPanel Spacing="4">
                            <TextBlock
                                Style="{StaticResource BodyStrongTextBlockStyle}"
                                Text="{x:Bind Label}"
                                TextWrapping="Wrap" />
                            <TextBlock IsTextSelectionEnabled="True" Text="{x:Bind Text}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="3" Padding="16" ColumnSpacing="8" RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Border
                Grid.ColumnSpan="3"
                Padding="12"
                Background="{ThemeResource SystemFillColorCautionBackgroundBrush}"
                BorderBrush="{ThemeResource SystemFillColorCautionBrush}"
                BorderThickness="1"
                CornerRadius="{StaticResource ControlCornerRadius}"
                Visibility="{x:Bind BoolToVisibility(ViewModel.HasPendingApproval), Mode=OneWay}">
                <StackPanel Spacing="8">
                    <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="This tool is awaiting your approval" TextWrapping="Wrap" />
                    <ScrollViewer MaxHeight="180" AutomationProperties.AutomationId="AssistantApprovalDetailsScroll">
                        <StackPanel Spacing="8">
                            <TextBlock Style="{StaticResource BodyStrongTextBlockStyle}" Text="{x:Bind ViewModel.PendingApproval.Summary, Mode=OneWay, FallbackValue=''}" TextWrapping="Wrap" />
                            <TextBlock IsTextSelectionEnabled="True" Text="{x:Bind ViewModel.PendingApproval.Details, Mode=OneWay, FallbackValue=''}" TextWrapping="Wrap" />
                        </StackPanel>
                    </ScrollViewer>
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Button AutomationProperties.AutomationId="AssistantApproveButton" Command="{x:Bind ViewModel.ApproveCommand}" Content="Approve" />
                        <Button AutomationProperties.AutomationId="AssistantRejectButton" Command="{x:Bind ViewModel.RejectCommand}" Content="Reject" />
                    </StackPanel>
                </StackPanel>
            </Border>
            <Grid
                Grid.Row="1"
                Grid.ColumnSpan="3"
                ColumnSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ProgressRing
                    AutomationProperties.AutomationId="AssistantProgressRing"
                    AutomationProperties.Name="Assistant request in progress"
                    Width="20"
                    Height="20"
                    IsActive="{x:Bind ViewModel.IsWorking, Mode=OneWay}"
                    Visibility="{x:Bind BoolToVisibility(ViewModel.IsWorking), Mode=OneWay}" />
                <TextBlock
                    AutomationProperties.AutomationId="AssistantStatusText"
                    AutomationProperties.LiveSetting="Polite"
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="{x:Bind ViewModel.StatusText, Mode=OneWay}"
                    TextWrapping="Wrap" />
            </Grid>
            <TextBox
                x:Name="DraftBox"
                AutomationProperties.AutomationId="AssistantDraftBox"
                AutomationProperties.Name="Message to container assistant"
                Grid.Row="2"
                AcceptsReturn="True"
                MaxHeight="96"
                PlaceholderText="Ask to list containers, deploy WordPress, stop all running containers…"
                PreviewKeyDown="DraftBox_PreviewKeyDown"
                Text="{x:Bind ViewModel.Draft, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                TextWrapping="Wrap" />
            <Button
                AutomationProperties.AutomationId="AssistantSendButton"
                Grid.Row="2"
                Grid.Column="1"
                Command="{x:Bind ViewModel.SendCommand}"
                Content="Send" />
            <Button
                AutomationProperties.AutomationId="AssistantCancelButton"
                Grid.Row="2"
                Grid.Column="2"
                Command="{x:Bind ViewModel.CancelCommand}"
                Content="Cancel"
                ToolTipService.ToolTip="Request cancellation. Completed actions are not rolled back." />
        </Grid>
    </Grid>
</UserControl>
