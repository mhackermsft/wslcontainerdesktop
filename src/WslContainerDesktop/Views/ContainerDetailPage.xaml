<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WslContainerDesktop.Views.ContainerDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WslContainerDesktop.Helpers"
    xmlns:models="using:WslContainerDesktop.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="Root"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StateToBrushConverter x:Key="StateToBrush" />
        <converters:StateToStringConverter x:Key="StateToString" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>

    <Grid Margin="24,14,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Breadcrumb / header  -->
        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <Button
                Grid.Column="0"
                Width="40"
                Height="40"
                Padding="0"
                VerticalAlignment="Center"
                Click="Back_Click"
                ToolTipService.ToolTip="Back to containers">
                <FontIcon FontSize="15" Glyph="&#xE72B;" />
            </Button>

            <StackPanel
                Grid.Column="1"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="12">
                <Border
                    Width="42"
                    Height="42"
                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <FontIcon
                        FontSize="19"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Glyph="&#xE7F4;" />
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <Ellipse
                            Width="12"
                            Height="12"
                            VerticalAlignment="Center"
                            Fill="{x:Bind ViewModel.Selected.State, Mode=OneWay, Converter={StaticResource StateToBrush}}" />
                        <TextBlock
                            FontSize="20"
                            FontWeight="SemiBold"
                            Text="{x:Bind ViewModel.Selected.Name, Mode=OneWay}" />
                        <Border
                            Padding="8,2"
                            VerticalAlignment="Center"
                            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                            CornerRadius="10">
                            <TextBlock
                                FontSize="12"
                                Text="{x:Bind ViewModel.Selected.State, Mode=OneWay, Converter={StaticResource StateToString}}" />
                        </Border>
                        <Border
                            Padding="8,2"
                            VerticalAlignment="Center"
                            Background="#1F7A3D"
                            CornerRadius="10"
                            ToolTipService.ToolTip="{x:Bind ViewModel.Selected.GpuTooltip, Mode=OneWay}"
                            Visibility="{x:Bind ViewModel.Selected.HasGpu, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                            <StackPanel Orientation="Horizontal" Spacing="4">
                                <FontIcon FontSize="12" Foreground="White" Glyph="&#xE950;" />
                                <TextBlock FontSize="12" Foreground="White" Text="GPU" />
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <TextBlock
                        FontSize="13"
                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                        Text="{x:Bind ViewModel.Selected.Image, Mode=OneWay}"
                        TextTrimming="CharacterEllipsis" />
                </StackPanel>
            </StackPanel>

            <!--  Actions  -->
            <StackPanel
                Grid.Column="2"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="6">
                <Button
                    Command="{x:Bind ViewModel.StartCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    Style="{ThemeResource AccentButtonStyle}"
                    ToolTipService.ToolTip="Start"
                    Visibility="{x:Bind ViewModel.Selected.IsStopped, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                    <FontIcon FontSize="14" Glyph="&#xE768;" />
                </Button>
                <Button
                    Command="{x:Bind ViewModel.StopCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    ToolTipService.ToolTip="Stop"
                    Visibility="{x:Bind ViewModel.Selected.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                    <FontIcon FontSize="14" Glyph="&#xE71A;" />
                </Button>
                <Button
                    Command="{x:Bind ViewModel.RestartCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    ToolTipService.ToolTip="Restart"
                    Visibility="{x:Bind ViewModel.Selected.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                    <FontIcon FontSize="14" Glyph="&#xE72C;" />
                </Button>
                <Button
                    Command="{x:Bind ViewModel.KillCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    ToolTipService.ToolTip="Kill (force stop)"
                    Visibility="{x:Bind ViewModel.Selected.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                    <FontIcon FontSize="14" Glyph="&#xE8BB;" />
                </Button>
                <Button
                    Command="{x:Bind ViewModel.TerminalCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    ToolTipService.ToolTip="Open terminal"
                    Visibility="{x:Bind ViewModel.Selected.IsRunning, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                    <FontIcon FontSize="14" Glyph="&#xE756;" />
                </Button>
                <Button
                    Command="{x:Bind ViewModel.OpenPortCommand}"
                    CommandParameter="{x:Bind ViewModel.Selected, Mode=OneWay}"
                    ToolTipService.ToolTip="Open published port in browser">
                    <FontIcon FontSize="14" Glyph="&#xE774;" />
                </Button>
                <Button ToolTipService.ToolTip="Remove" Click="Remove_Click">
                    <FontIcon FontSize="14" Glyph="&#xE74D;" />
                </Button>
            </StackPanel>
        </Grid>

        <Border
            Grid.Row="1"
            Height="1"
            Background="{ThemeResource DividerStrokeColorDefaultBrush}" />

        <!--  Tabs  -->
        <SelectorBar
            x:Name="DetailTabs"
            Grid.Row="2"
            SelectionChanged="DetailTabs_SelectionChanged">
            <SelectorBarItem
                x:Name="TabLogs"
                IsSelected="True"
                Text="Logs">
                <SelectorBarItem.Icon>
                    <FontIcon Glyph="&#xE9D9;" />
                </SelectorBarItem.Icon>
            </SelectorBarItem>
            <SelectorBarItem x:Name="TabSummary" Text="Summary">
                <SelectorBarItem.Icon>
                    <FontIcon Glyph="&#xE946;" />
                </SelectorBarItem.Icon>
            </SelectorBarItem>
            <SelectorBarItem x:Name="TabStats" Text="Stats">
                <SelectorBarItem.Icon>
                    <FontIcon Glyph="&#xE9D9;" />
                </SelectorBarItem.Icon>
            </SelectorBarItem>
            <SelectorBarItem x:Name="TabInspect" Text="Inspect">
                <SelectorBarItem.Icon>
                    <FontIcon Glyph="&#xE943;" />
                </SelectorBarItem.Icon>
            </SelectorBarItem>
            <SelectorBarItem x:Name="TabFiles" Text="Files">
                <SelectorBarItem.Icon>
                    <FontIcon Glyph="&#xE838;" />
                </SelectorBarItem.Icon>
            </SelectorBarItem>
        </SelectorBar>

        <!--  Tab content  -->
        <Grid Grid.Row="3">
            <!--  ===== Logs (default, full height) =====  -->
            <Grid x:Name="LogsPanel" RowSpacing="8">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <StackPanel
                    Grid.Row="0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal"
                    Spacing="8">
                    <ToggleButton
                        x:Name="WrapToggle"
                        Click="WrapToggle_Click"
                        Content="Wrap" />
                    <Button Click="ClearLogs_Click" Content="Clear" />
                </StackPanel>

                <Border
                    Grid.Row="1"
                    Background="#0C0C0F"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
                    <ScrollViewer
                        x:Name="LogScroll"
                        Padding="14,10"
                        HorizontalScrollBarVisibility="Auto"
                        HorizontalScrollMode="Auto"
                        VerticalScrollBarVisibility="Auto">
                        <TextBlock
                            x:Name="LogText"
                            FontFamily="Cascadia Mono, Consolas"
                            FontSize="13"
                            Foreground="#E4E4E8"
                            IsTextSelectionEnabled="True"
                            LineHeight="19"
                            TextWrapping="NoWrap" />
                    </ScrollViewer>
                </Border>
            </Grid>

            <!--  ===== Summary =====  -->
            <ScrollViewer x:Name="SummaryPanel" Visibility="Collapsed">
                <StackPanel MaxWidth="900" Spacing="12">
                    <Border
                        Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="6">
                        <Grid ColumnSpacing="24" RowSpacing="10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Container ID" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.Selected.ShortId, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="State" />
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                Text="{x:Bind ViewModel.Selected.State, Mode=OneWay, Converter={StaticResource StateToString}}" />

                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Image" />
                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="1"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.Selected.Image, Mode=OneWay}"
                                TextWrapping="Wrap" />

                            <TextBlock
                                Grid.Row="3"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Ports" />
                            <TextBlock
                                Grid.Row="3"
                                Grid.Column="1"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.Selected.PortsDisplay, Mode=OneWay}"
                                TextWrapping="Wrap" />

                            <TextBlock
                                Grid.Row="4"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="IP address" />
                            <TextBlock
                                Grid.Row="4"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.DetailIp, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="5"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Network" />
                            <TextBlock
                                Grid.Row="5"
                                Grid.Column="1"
                                Text="{x:Bind ViewModel.DetailNetwork, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="6"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Started" />
                            <TextBlock
                                Grid.Row="6"
                                Grid.Column="1"
                                Text="{x:Bind ViewModel.DetailStarted, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="7"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Command" />
                            <TextBlock
                                Grid.Row="7"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                FontSize="12"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.DetailCommand, Mode=OneWay}"
                                TextWrapping="Wrap" />
                        </Grid>
                    </Border>

                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Header="Environment variables">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.DetailEnvironment, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <TextBlock
                                        Margin="0,2"
                                        FontFamily="Consolas"
                                        FontSize="12"
                                        IsTextSelectionEnabled="True"
                                        Text="{x:Bind}"
                                        TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                    <Expander
                        HorizontalAlignment="Stretch"
                        HorizontalContentAlignment="Stretch"
                        Header="Mounts">
                        <ItemsControl ItemsSource="{x:Bind ViewModel.DetailMounts, Mode=OneWay}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="x:String">
                                    <TextBlock
                                        Margin="0,2"
                                        FontFamily="Consolas"
                                        FontSize="12"
                                        IsTextSelectionEnabled="True"
                                        Text="{x:Bind}"
                                        TextWrapping="Wrap" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander>
                </StackPanel>
            </ScrollViewer>

            <!--  ===== Stats =====  -->
            <ScrollViewer x:Name="StatsPanel" Visibility="Collapsed">
                <StackPanel MaxWidth="900" Spacing="14">
                    <!--  CPU + Memory big meters  -->
                    <Grid ColumnSpacing="14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <Border
                            Grid.Column="0"
                            Padding="16"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="16"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xE950;" />
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="CPU" />
                                </StackPanel>
                                <TextBlock FontSize="30" FontWeight="SemiBold" Text="{x:Bind ViewModel.StatCpu, Mode=OneWay}" />
                                <ProgressBar Maximum="100" Value="{x:Bind ViewModel.StatCpuValue, Mode=OneWay}" />
                            </StackPanel>
                        </Border>

                        <Border
                            Grid.Column="1"
                            Padding="16"
                            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                            BorderThickness="1"
                            CornerRadius="8">
                            <StackPanel Spacing="8">
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon
                                        FontSize="16"
                                        Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                        Glyph="&#xEEA0;" />
                                    <TextBlock Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="Memory" />
                                </StackPanel>
                                <TextBlock FontSize="30" FontWeight="SemiBold" Text="{x:Bind ViewModel.StatMem, Mode=OneWay}" />
                                <ProgressBar Maximum="100" Value="{x:Bind ViewModel.StatMemValue, Mode=OneWay}" />
                                <TextBlock
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind ViewModel.StatMemUsage, Mode=OneWay}" />
                            </StackPanel>
                        </Border>
                    </Grid>

                    <!--  I/O + PIDs  -->
                    <Border
                        Padding="16"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8">
                        <Grid ColumnSpacing="24" RowSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Network I/O" />
                            <TextBlock
                                Grid.Row="0"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                Text="{x:Bind ViewModel.StatNetIO, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Block I/O" />
                            <TextBlock
                                Grid.Row="1"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                Text="{x:Bind ViewModel.StatBlockIO, Mode=OneWay}" />

                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="0"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="Processes (PIDs)" />
                            <TextBlock
                                Grid.Row="2"
                                Grid.Column="1"
                                FontFamily="Consolas"
                                Text="{x:Bind ViewModel.StatPids, Mode=OneWay}" />
                        </Grid>
                    </Border>

                    <TextBlock
                        FontSize="12"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Text="Updates every 2 seconds. Stats are only available while the container is running." />
                </StackPanel>
            </ScrollViewer>

            <!--  ===== Files =====  -->
            <Grid
                x:Name="FilesPanel"
                RowSpacing="10"
                Visibility="Collapsed">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="220" />
                </Grid.RowDefinitions>

                <Border
                    Padding="12"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <StackPanel Spacing="8">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button
                                Click="FilesUp_Click"
                                Content="Up"
                                IsEnabled="{x:Bind ViewModel.CanNavigateUp, Mode=OneWay}" />
                            <Button Click="FilesRefresh_Click" Content="Refresh" />
                            <Button Click="CopyInFile_Click" Content="Copy file in" />
                            <Button Click="CopyInFolder_Click" Content="Copy folder in" />
                            <Button
                                Click="CopyOut_Click"
                                Content="Copy out"
                                IsEnabled="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay}" />
                            <Button
                                Click="PreviewFile_Click"
                                Content="View"
                                IsEnabled="{x:Bind ViewModel.CanPreviewSelectedFile, Mode=OneWay}" />
                            <Button
                                Click="DeleteFile_Click"
                                Content="Delete"
                                IsEnabled="{x:Bind ViewModel.HasSelectedFile, Mode=OneWay}" />
                        </StackPanel>
                        <TextBlock
                            FontFamily="Consolas"
                            FontSize="13"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind ViewModel.FilesCurrentPath, Mode=OneWay}" />
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            Text="{x:Bind ViewModel.FilesStatusMessage, Mode=OneWay}"
                            TextWrapping="Wrap" />
                        <TextBlock
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                            Text="Click a folder to drill down. Preview supports small files up to 64 KB." />
                    </StackPanel>
                </Border>

                <Border
                    Grid.Row="1"
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Grid
                            Padding="12,10"
                            ColumnSpacing="12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="36" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="110" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="90" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                Grid.Column="1"
                                FontWeight="SemiBold"
                                Text="Name" />
                            <TextBlock
                                Grid.Column="2"
                                FontWeight="SemiBold"
                                Text="Permissions" />
                            <TextBlock
                                Grid.Column="3"
                                FontWeight="SemiBold"
                                Text="Owner" />
                            <TextBlock
                                Grid.Column="4"
                                FontWeight="SemiBold"
                                Text="Modified" />
                            <TextBlock
                                Grid.Column="5"
                                FontWeight="SemiBold"
                                Text="Size" />
                        </Grid>

                        <ListView
                            x:Name="FilesList"
                            Grid.Row="1"
                            IsItemClickEnabled="True"
                            ItemsSource="{x:Bind ViewModel.DetailFiles, Mode=OneWay}"
                            ItemClick="FilesList_ItemClick"
                            SelectedItem="{x:Bind ViewModel.SelectedFile, Mode=TwoWay}"
                            SelectionMode="Single">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="models:ContainerFileEntry">
                                    <Grid
                                        Padding="12,8"
                                        ColumnSpacing="12">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="36" />
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="110" />
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="150" />
                                            <ColumnDefinition Width="90" />
                                        </Grid.ColumnDefinitions>

                                        <FontIcon
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            Glyph="{x:Bind IconGlyph}" />
                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind Name}"
                                            TextTrimming="CharacterEllipsis" />
                                        <TextBlock
                                            Grid.Column="2"
                                            FontFamily="Consolas"
                                            FontSize="12"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind Permissions}" />
                                        <TextBlock
                                            Grid.Column="3"
                                            FontFamily="Consolas"
                                            FontSize="12"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind OwnerDisplay}" />
                                        <TextBlock
                                            Grid.Column="4"
                                            FontSize="12"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind ModifiedDisplay}" />
                                        <TextBlock
                                            Grid.Column="5"
                                            FontSize="12"
                                            VerticalAlignment="Center"
                                            Text="{x:Bind SizeDisplay}" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </Border>

                <Border
                    Grid.Row="2"
                    Background="#0C0C0F"
                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <StackPanel
                            Padding="12,10,12,0"
                            Spacing="4">
                            <TextBlock FontWeight="SemiBold" Text="Preview" />
                            <TextBlock
                                FontFamily="Consolas"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{x:Bind ViewModel.FilePreviewTitle, Mode=OneWay}"
                                TextTrimming="CharacterEllipsis" />
                        </StackPanel>

                        <ScrollViewer
                            Grid.Row="1"
                            Padding="12,8,12,12"
                            HorizontalScrollBarVisibility="Auto"
                            HorizontalScrollMode="Auto"
                            VerticalScrollBarVisibility="Auto">
                            <TextBlock
                                FontFamily="Cascadia Mono, Consolas"
                                FontSize="12"
                                Foreground="#E4E4E8"
                                IsTextSelectionEnabled="True"
                                Text="{x:Bind ViewModel.FilePreviewText, Mode=OneWay}"
                                TextWrapping="NoWrap" />
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>

            <!--  ===== Inspect =====  -->
            <Border
                x:Name="InspectPanel"
                Background="#0C0C0F"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="6"
                Visibility="Collapsed">
                <ScrollViewer
                    Padding="14,10"
                    HorizontalScrollBarVisibility="Auto"
                    HorizontalScrollMode="Auto"
                    VerticalScrollBarVisibility="Auto">
                    <TextBlock
                        FontFamily="Cascadia Mono, Consolas"
                        FontSize="12"
                        Foreground="#E4E4E8"
                        IsTextSelectionEnabled="True"
                        Text="{x:Bind ViewModel.DetailInspectJson, Mode=OneWay}"
                        TextWrapping="NoWrap" />
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
