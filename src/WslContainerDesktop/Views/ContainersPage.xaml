<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WslContainerDesktop.Views.ContainersPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WslContainerDesktop.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WslContainerDesktop.ViewModels"
    x:Name="Root"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:StateToBrushConverter x:Key="StateToBrush" />
        <converters:StateToStringConverter x:Key="StateToString" />
        <converters:RelativeTimeConverter x:Key="RelativeTime" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:EmptyToVisibilityConverter x:Key="EmptyToVis" />
    </Page.Resources>

    <Grid Margin="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="Containers" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>

        <!--  Toolbar  -->
        <Grid Grid.Row="1">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button Command="{x:Bind ViewModel.RunCommand}" Style="{ThemeResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="14" Glyph="&#xE768;" />
                        <TextBlock Text="Run a container" />
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.RefreshCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="14" Glyph="&#xE72C;" />
                        <TextBlock Text="Refresh" />
                    </StackPanel>
                </Button>
                <Button Command="{x:Bind ViewModel.PruneCommand}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon FontSize="14" Glyph="&#xE74D;" />
                        <TextBlock Text="Prune stopped" />
                    </StackPanel>
                </Button>
            </StackPanel>
            <StackPanel
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <TextBlock VerticalAlignment="Center" Text="Show all" />
                <ToggleSwitch MinWidth="0" IsOn="{x:Bind ViewModel.ShowAll, Mode=TwoWay}" />
            </StackPanel>
        </Grid>

        <!--  Column header  -->
        <Border
            Grid.Row="2"
            Padding="16,8,20,8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="6,6,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="14" />
                    <ColumnDefinition Width="1.8*" MinWidth="110" />
                    <ColumnDefinition Width="1.9*" MinWidth="110" />
                    <ColumnDefinition Width="1.2*" MinWidth="80" />
                    <ColumnDefinition Width="1.1*" MinWidth="80" />
                    <ColumnDefinition Width="84" />
                    <ColumnDefinition Width="156" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="1" Margin="10,0,0,0" Style="{ThemeResource CaptionTextBlockStyle}" Text="NAME" />
                <TextBlock Grid.Column="2" Style="{ThemeResource CaptionTextBlockStyle}" Text="IMAGE" />
                <TextBlock Grid.Column="3" Style="{ThemeResource CaptionTextBlockStyle}" Text="PORTS" />
                <TextBlock Grid.Column="4" Style="{ThemeResource CaptionTextBlockStyle}" Text="NETWORK" />
                <TextBlock Grid.Column="5" Style="{ThemeResource CaptionTextBlockStyle}" Text="CREATED" />
                <TextBlock
                    Grid.Column="6"
                    HorizontalAlignment="Right"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="ACTIONS" />
            </Grid>
        </Border>

        <!--  List  -->
        <Border
            Grid.Row="3"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,6,6">
            <Grid>
                <ListView
                    x:Name="ContainersList"
                    Padding="0,4"
                    IsItemClickEnabled="True"
                    ItemClick="ContainersList_ItemClick"
                    ItemsSource="{x:Bind ViewModel.Containers, Mode=OneWay}"
                    SelectionMode="None">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="16,6,20,6" />
                            <Setter Property="MinHeight" Value="56" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="vm:ContainerRowViewModel">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="14" />
                                    <ColumnDefinition Width="1.8*" MinWidth="110" />
                                    <ColumnDefinition Width="1.9*" MinWidth="110" />
                                    <ColumnDefinition Width="1.2*" MinWidth="80" />
                                    <ColumnDefinition Width="1.1*" MinWidth="80" />
                                    <ColumnDefinition Width="84" />
                                    <ColumnDefinition Width="156" />
                                </Grid.ColumnDefinitions>

                                <Ellipse
                                    Grid.Column="0"
                                    Width="12"
                                    Height="12"
                                    VerticalAlignment="Center"
                                    Fill="{Binding State, Converter={StaticResource StateToBrush}}"
                                    ToolTipService.ToolTip="{Binding State, Converter={StaticResource StateToString}}" />

                                <StackPanel
                                    Grid.Column="1"
                                    Margin="10,0,8,0"
                                    VerticalAlignment="Center"
                                    Spacing="2">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <TextBlock
                                            VerticalAlignment="Center"
                                            FontWeight="SemiBold"
                                            Text="{Binding Name}"
                                            TextTrimming="CharacterEllipsis" />
                                        <Border
                                            Padding="5,1"
                                            VerticalAlignment="Center"
                                            Background="#1F7A3D"
                                            CornerRadius="4"
                                            ToolTipService.ToolTip="{Binding GpuTooltip}"
                                            Visibility="{Binding HasGpu, Converter={StaticResource BoolToVis}}">
                                            <StackPanel Orientation="Horizontal" Spacing="3">
                                                <FontIcon FontSize="10" Foreground="White" Glyph="&#xE950;" />
                                                <TextBlock FontSize="10" Foreground="White" Text="GPU" />
                                            </StackPanel>
                                        </Border>
                                    </StackPanel>
                                    <TextBlock
                                        FontFamily="Consolas"
                                        FontSize="11"
                                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                        Text="{Binding ShortId}" />
                                </StackPanel>

                                <TextBlock
                                    Grid.Column="2"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Image}"
                                    TextTrimming="CharacterEllipsis" />

                                <TextBlock
                                    Grid.Column="3"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Text="{Binding PortsDisplay}"
                                    TextTrimming="CharacterEllipsis" />

                                <TextBlock
                                    Grid.Column="4"
                                    Margin="0,0,8,0"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Network}"
                                    TextTrimming="CharacterEllipsis" />

                                <TextBlock
                                    Grid.Column="5"
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Created, Converter={StaticResource RelativeTime}}" />

                                <StackPanel
                                    Grid.Column="6"
                                    HorizontalAlignment="Right"
                                    VerticalAlignment="Center"
                                    Orientation="Horizontal"
                                    Spacing="2">
                                    <Button
                                        Width="34"
                                        Height="34"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="StartButton_Click"
                                        ToolTipService.ToolTip="Start"
                                        Visibility="{Binding IsStopped, Converter={StaticResource BoolToVis}}">
                                        <FontIcon FontSize="15" Glyph="&#xE768;" />
                                    </Button>
                                    <Button
                                        Width="34"
                                        Height="34"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="StopButton_Click"
                                        ToolTipService.ToolTip="Stop"
                                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                                        <FontIcon FontSize="15" Glyph="&#xE71A;" />
                                    </Button>
                                    <Button
                                        Width="34"
                                        Height="34"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="RestartButton_Click"
                                        ToolTipService.ToolTip="Restart"
                                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                                        <FontIcon FontSize="15" Glyph="&#xE72C;" />
                                    </Button>
                                    <Button
                                        Width="34"
                                        Height="34"
                                        Padding="0"
                                        AutomationProperties.Name="Terminal"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Click="TerminalButton_Click"
                                        ToolTipService.ToolTip="Terminal"
                                        Visibility="{Binding IsRunning, Converter={StaticResource BoolToVis}}">
                                        <FontIcon FontSize="15" Glyph="&#xE756;" />
                                    </Button>
                                    <Button
                                        Width="34"
                                        Height="34"
                                        Padding="0"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        ToolTipService.ToolTip="More">
                                        <FontIcon FontSize="15" Glyph="&#xE712;" />
                                        <Button.Flyout>
                                            <MenuFlyout>
                                                <MenuFlyoutItem Click="OpenPortMenu_Click" Text="Open in browser">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE774;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                                <MenuFlyoutItem Click="KillMenu_Click" Text="Kill" />
                                                <MenuFlyoutSeparator />
                                                <MenuFlyoutItem Click="RemoveMenu_Click" Text="Remove">
                                                    <MenuFlyoutItem.Icon>
                                                        <FontIcon Glyph="&#xE74D;" />
                                                    </MenuFlyoutItem.Icon>
                                                </MenuFlyoutItem>
                                            </MenuFlyout>
                                        </Button.Flyout>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!--  Empty state  -->
                <StackPanel
                    Margin="0,70,0,0"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    Spacing="10"
                    Visibility="{x:Bind ViewModel.Containers.Count, Mode=OneWay, Converter={StaticResource EmptyToVis}}">
                    <FontIcon
                        HorizontalAlignment="Center"
                        FontSize="44"
                        Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                        Glyph="&#xE7F4;" />
                    <TextBlock
                        HorizontalAlignment="Center"
                        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                        Text="No containers yet. Click &quot;Run a container&quot; to get started." />
                </StackPanel>

                <ProgressBar
                    VerticalAlignment="Top"
                    IsIndeterminate="True"
                    Visibility="{x:Bind ViewModel.IsBusy, Mode=OneWay, Converter={StaticResource BoolToVis}}" />
            </Grid>
        </Border>
    </Grid>
</Page>
