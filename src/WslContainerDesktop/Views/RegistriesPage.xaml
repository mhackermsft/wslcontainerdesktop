<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="WslContainerDesktop.Views.RegistriesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:WslContainerDesktop.Helpers"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WslContainerDesktop.Models"
    x:Name="Root"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVis" />
        <converters:HexToBrushConverter x:Key="HexToBrush" />
    </Page.Resources>

    <Grid Margin="24,16,24,16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!--  Header  -->
        <StackPanel Grid.Row="0" Spacing="2">
            <TextBlock Style="{ThemeResource TitleTextBlockStyle}" Text="Registries" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{ThemeResource CaptionTextBlockStyle}"
                Text="{x:Bind ViewModel.StatusMessage, Mode=OneWay}" />
        </StackPanel>

        <!--  Toolbar  -->
        <StackPanel
            Grid.Row="1"
            Orientation="Horizontal"
            Spacing="8">
            <Button Command="{x:Bind ViewModel.AddCommand}" Style="{ThemeResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE710;" />
                    <TextBlock Text="Add registry" />
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.AddFromAzureCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE753;" />
                    <TextBlock Text="Add from Azure" />
                </StackPanel>
            </Button>
            <Button Command="{x:Bind ViewModel.RefreshLoginStatesCommand}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon FontSize="14" Glyph="&#xE72C;" />
                    <TextBlock Text="Refresh status" />
                </StackPanel>
            </Button>
        </StackPanel>

        <!--  Column header  -->
        <Border
            Grid.Row="2"
            Padding="16,8,20,8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="6,6,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" MinWidth="140" />
                    <ColumnDefinition Width="2.2*" MinWidth="160" />
                    <ColumnDefinition Width="1.2*" MinWidth="100" />
                    <ColumnDefinition Width="150" />
                    <ColumnDefinition Width="200" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{ThemeResource CaptionTextBlockStyle}" Text="NAME" />
                <TextBlock Grid.Column="1" Style="{ThemeResource CaptionTextBlockStyle}" Text="HOST" />
                <TextBlock Grid.Column="2" Style="{ThemeResource CaptionTextBlockStyle}" Text="USERNAME" />
                <TextBlock Grid.Column="3" Style="{ThemeResource CaptionTextBlockStyle}" Text="STATUS" />
                <TextBlock
                    Grid.Column="4"
                    HorizontalAlignment="Right"
                    Style="{ThemeResource CaptionTextBlockStyle}"
                    Text="ACTIONS" />
            </Grid>
        </Border>

        <!--  List  -->
        <Border
            Grid.Row="3"
            Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
            BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
            BorderThickness="1,0,1,1"
            CornerRadius="0,0,6,6">
            <ListView
                Padding="0,4"
                ItemsSource="{x:Bind ViewModel.Registries, Mode=OneWay}"
                SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="16,6,20,6" />
                        <Setter Property="MinHeight" Value="52" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:RegistryEntry">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" MinWidth="140" />
                                <ColumnDefinition Width="2.2*" MinWidth="160" />
                                <ColumnDefinition Width="1.2*" MinWidth="100" />
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="200" />
                            </Grid.ColumnDefinitions>

                            <StackPanel
                                Grid.Column="0"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="8">
                                <FontIcon FontSize="15" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Glyph="&#xE753;" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontWeight="SemiBold"
                                    Text="{x:Bind Name}"
                                    TextTrimming="CharacterEllipsis" />
                                <Border
                                    Padding="8,1"
                                    VerticalAlignment="Center"
                                    Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                    CornerRadius="8"
                                    Visibility="{x:Bind IsDefault, Converter={StaticResource BoolToVis}}">
                                    <TextBlock FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Text="default" />
                                </Border>
                            </StackPanel>

                            <TextBlock
                                Grid.Column="1"
                                VerticalAlignment="Center"
                                FontFamily="Consolas"
                                FontSize="12"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{x:Bind Host}"
                                TextTrimming="CharacterEllipsis" />

                            <TextBlock
                                Grid.Column="2"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                Text="{x:Bind Username}"
                                TextTrimming="CharacterEllipsis" />

                            <!--  Status: only meaningful for non-default registries  -->
                            <StackPanel
                                Grid.Column="3"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="7"
                                Visibility="{x:Bind IsDefault, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                                <FontIcon
                                    FontSize="13"
                                    Foreground="{x:Bind LoginStateColor, Mode=OneWay, Converter={StaticResource HexToBrush}}"
                                    Glyph="{x:Bind LoginStateGlyph, Mode=OneWay}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontSize="12"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{x:Bind LoginStateText, Mode=OneWay}" />
                            </StackPanel>

                            <!--  Actions: only for non-default registries  -->
                            <StackPanel
                                Grid.Column="4"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="4"
                                Visibility="{x:Bind IsDefault, Converter={StaticResource BoolToVis}, ConverterParameter=invert}">
                                <Button Click="Login_Click" AutomationProperties.Name="Log in" ToolTipService.ToolTip="Log in / re-authenticate" Visibility="{x:Bind ShowLoginButton, Mode=OneWay, Converter={StaticResource BoolToVis}}">
                                    <StackPanel Orientation="Horizontal" Spacing="6">
                                        <FontIcon FontSize="13" Glyph="&#xE77B;" />
                                        <TextBlock FontSize="13" Text="Log in" />
                                    </StackPanel>
                                </Button>
                                <Button Click="Remove_Click" AutomationProperties.Name="Remove" ToolTipService.ToolTip="Remove">
                                    <FontIcon FontSize="13" Glyph="&#xE74D;" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
