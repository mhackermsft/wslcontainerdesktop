# Synthetic GPL-3.0-or-later fixture.
services:
  web:
    image: example.invalid/conformance:1
    networks:
      public:
        aliases:
          - frontend
      private:
        aliases:
          - backend
        ipv4_address: 192.0.2.10
networks:
  public:
  private:
    ipam:
      config:
        - subnet: 192.0.2.0/24
